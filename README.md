# dots
My dotfiles.

`make install` will install all files with newer timestamps than the ones in
your home directory.

`make reset` **WILL REMOVE** `~/.config` and any file starting with a dot
immediately in your home directory.
